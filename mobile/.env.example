# Requires a confirmed native-compatible backend and authentication provider.
# Do not put passwords, privileged API keys or Sites bypass tokens here.
VITE_SAMBU_API_ORIGIN=
VITE_SAMBU_NATIVE_AUTH_READY=false
